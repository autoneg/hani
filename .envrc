source /Users/yasser/myvenvs/han/bin/activate

# Source local environment variables (secrets) if they exist
# This file is gitignored and should contain your OAuth credentials
if [ -f .envrc.local ]; then
  source_env .envrc.local
fi
